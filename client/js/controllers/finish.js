app.controller('Finish', ($scope, $http, $cookies) => {

	// const initCart = () =>$scope.cart = $scope.user.orders.find(order=>$cookies.get('cart') === order._id);
	
	// const initPage = () =>{
	// 	initCart();
	// }

	// initPage();
});